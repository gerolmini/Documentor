<?php
/**
* Librería de funciones de test.
*
* @author Gustavo <Gustavo@mail.com>
*
*/
namespace Lib\arrays;

/**
* @param int $array declaramos cual es la cadena de números que vamos a analizar.
*
*/
/**
* Función que busca el número mayor de un array.
*
* @param int $array de números enteros de la cual queremos saber cual es el mayor.
* @return int $max Devuelve el número mayor del array.
*/
function max($array){
  return 0;
}

/**
* Función que busque el mínimo de un array de enteros recibido como argumento.
*
* @param int $array de números enteros de la cual queremos saber cual es el menor.
* @return int $max Devuelve el número menor del array.
*/
function min($array){
  return 0;
}

/**
* Función que secuencia mayor en un array.
*
* Una sección de "espejo" en una matriz es un grupo de elementos contiguos tales que en algún lugar de la matriz, el mismo grupo aparece en el orden inverso.
* @param int $array variable que recibe la función.
* @return int Devolver el tamaño de la sección espejo más grande que se encuentra en la matriz dada.
*/
function espejoMayor($array){
  return 0;
}
?>
